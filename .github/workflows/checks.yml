name: checks

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    name: Repo and installer checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Tracked files are plain ASCII (no em dashes, en dashes, or emojis)
        run: python3 .github/scripts/check-no-fancy-chars.py

      - name: One name only (no stray source skill name)
        run: |
          TERM="code""auditor"
          if git grep -InF "$TERM" -- .; then
            echo "::error::Found a stray reference to the source skill name. The skill is named uxauditor."
            exit 1
          fi
          echo "clean: single name uxauditor"

      - name: Installer is valid bash
        run: bash -n install.sh

      - name: Engine and installer are consistent
        run: |
          test -f engine/uxauditor.md
          head -n 1 engine/uxauditor.md | grep -q '^# uxauditor'
          grep -qF 'engine/$NAME.md' install.sh
          echo "ok"
